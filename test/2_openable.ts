import { Openable } from '../provide'

contract('Openable', async accounts => {
    it('should create an Openable', async () => {
        const openableInstance = await Openable.new()
        await openableInstance.
    })
})